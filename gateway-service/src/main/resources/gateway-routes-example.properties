# Gateway 路由示例配置（参考用，未被 Spring 自动加载）

# 路由示例 - 用户服务
gateway.example.routes[0].id=example-user
gateway.example.routes[0].uri=lb://user-service
gateway.example.routes[0].predicates[0]=Path=/example/user/**
gateway.example.routes[0].filters[0]=StripPrefix=2
